import 'package:flutter/material.dart';

class ListChatPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Halaman Daftar Chat', style: TextStyle(fontSize: 20)),
    );
  }
}
