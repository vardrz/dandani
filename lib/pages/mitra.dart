import 'package:flutter/material.dart';

class MitraPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Halaman Pendaftaran Jasa',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
